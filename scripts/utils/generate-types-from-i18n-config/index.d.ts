import type { Config as PrettierConfig } from 'prettier'
import type { LocaleConfig } from '../../../types/locale'

export function generateTypesFromI18nConfig(typeDefFilePath: string, localeConfig: LocaleConfig, prettierConfig: PrettierConfig): void
